# For some reason, I cannot do inheritance
mirror_dir: {{ mirror_dir }}/r
platforms:
  - osx-64
  - win-64
  - linux-64
  - noarch
fetch_installers: False
verify_checksum: True
log_dir: {{ log_dir }}
log_level: {{ log_level }}
channels:
  - https://repo.anaconda.com/pkgs/r/
